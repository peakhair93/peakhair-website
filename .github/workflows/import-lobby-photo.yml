name: Import clinic lobby photo
on:
  workflow_dispatch:
    inputs:
      image_url:
        description: Temporary client photo link
        required: true
permissions:
  contents: write
jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install pillow
      - name: Save lobby photo
        env:
          IMAGE_URL: ${{ inputs.image_url }}
        run: |
          python - <<'PY'
          import io, os, urllib.request, urllib.parse
          from PIL import Image, ImageOps
          url=os.environ['IMAGE_URL']
          parsed=urllib.parse.urlparse(url)
          if parsed.scheme!='https' or parsed.hostname!='gwvckixiegkllthleuyt.supabase.co' or not parsed.path.startswith('/storage/v1/object/sign/paid-ads-chat-files/'):
              raise ValueError('Unexpected image link')
          with urllib.request.urlopen(url, timeout=45) as response:
              raw=response.read(12000000)
          image=ImageOps.exif_transpose(Image.open(io.BytesIO(raw))).convert('RGB')
          image.thumbnail((1500,1500),Image.Resampling.LANCZOS)
          os.makedirs('public/assets',exist_ok=True)
          image.save('public/assets/peak-nyc-clinic-lobby-2.jpg','JPEG',quality=85,optimize=True)
          print('Saved lobby photo', image.size)
          PY
      - name: Commit photo
        run: |
          git config user.name 'Peak Photo Import'
          git config user.email 'photo-import@users.noreply.github.com'
          git add public/assets/peak-nyc-clinic-lobby-2.jpg
          git commit -m 'Add second Peak New York lobby photo'
          git push
